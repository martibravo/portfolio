<div class="case-system" aria-label="The campaign visual system">
  <div class="case-system-sequence">
    <article>
      <div class="case-system-stage case-system-module" aria-hidden="true">
        <span></span><span></span><span></span><span></span>
      </div>
      <h3>Everyday module</h3>
      <p>Taken from the Bicing mark.</p>
    </article>
    <article>
      <div class="case-system-stage case-system-grid" aria-hidden="true">
        {Array.from({ length: 9 }).map(() => <span></span>)}
      </div>
      <h3>Urban grid</h3>
      <p>Repeated at the rhythm of an Eixample block.</p>
    </article>
    <article>
      <div class="case-system-stage case-system-photo">
        <img src="/images/graphic-design/bicing/system-infrastructure.webp" alt="Bicing bicycles parked at a public station" width="800" height="609" loading="lazy" />
      </div>
      <h3>Public infrastructure</h3>
      <p>The service as part of the street.</p>
    </article>
    <article>
      <div class="case-system-stage case-system-route" aria-hidden="true">
        <span></span><span></span><span></span><span></span>
        <svg viewBox="0 0 240 180">
          <path d="M-20 128 H84 L120 92 H170 L214 48 H260" />
        </svg>
      </div>
      <h3>Personal route</h3>
      <p>One line for each journey.</p>
    </article>
  </div>

  <div class="case-system-foundations">
    <div class="case-system-palette">
      <span class="red"><b>Bicing red</b><small>Route · action · brand</small></span>
      <span class="ink"><b>Asphalt</b><small>Type · contrast</small></span>
      <span class="paper"><b>Paper</b><small>Space · clarity</small></span>
      <span class="gray"><b>Infrastructure</b><small>Grid · secondary information</small></span>
    </div>
    <div class="case-system-type">
      <span>Campaign type</span>
      <strong>Helvetica Neue</strong>
      <p lang="ca">Fes teva la ciutat.</p>
      <em>Make the city yours.</em>
    </div>
  </div>
</div>

<style>
  .case-system {
    margin-top: clamp(3rem, 7vw, 6rem);
    border: 1px solid var(--case-line);
    background: var(--case-paper);
    box-shadow: var(--case-image-shadow);
  }
  .case-system-sequence {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
  }
  article { min-width: 0; padding: clamp(0.75rem, 2vw, 1.35rem); }
  article + article { border-left: 1px solid var(--case-line); }
  .case-system-stage {
    position: relative;
    display: grid;
    aspect-ratio: 1;
    overflow: hidden;
    background: var(--case-surface);
  }
  h3 { margin: 1rem 0 0; font-size: 0.85rem; font-weight: 600; line-height: 1.2; }
  article p { margin: 0.4rem 0 0; color: var(--case-gray); font-size: 11px; line-height: 1.35; }
  .case-system-module span {
    position: absolute;
    left: 50%;
    top: 50%;
    width: 38%;
    max-width: 90px;
    aspect-ratio: 1;
    border: clamp(7px, 1.1vw, 14px) solid var(--case-ink);
    border-radius: 50%;
  }
  .case-system-module span:nth-child(1) { transform: translate(-92%, -92%); }
  .case-system-module span:nth-child(2) { transform: translate(-8%, -92%); }
  .case-system-module span:nth-child(3) { transform: translate(-92%, -8%); }
  .case-system-module span:nth-child(4) { transform: translate(-8%, -8%); }
  .case-system-grid,
  .case-system-route {
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(3, 1fr);
    gap: 7%;
    padding: 8%;
  }
  .case-system-grid span,
  .case-system-route span {
    background: var(--case-paper);
    clip-path: polygon(16% 0, 84% 0, 100% 16%, 100% 84%, 84% 100%, 16% 100%, 0 84%, 0 16%);
  }
  .case-system-photo img { width: 100%; height: 100%; object-fit: cover; }
  .case-system-route span:nth-child(1) { grid-area: 1 / 1; }
  .case-system-route span:nth-child(2) { grid-area: 1 / 3; }
  .case-system-route span:nth-child(3) { grid-area: 3 / 1; }
  .case-system-route span:nth-child(4) { grid-area: 3 / 3; }
  .case-system-route svg { position: absolute; inset: 0; width: 100%; height: 100%; }
  .case-system-route path {
    fill: none;
    stroke: var(--case-red);
    stroke-width: 12;
    stroke-linecap: round;
    stroke-linejoin: round;
  }
  .case-system-foundations {
    display: grid;
    grid-template-columns: 1.5fr 1fr;
    border-top: 1px solid var(--case-line);
  }
  .case-system-palette {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    min-height: 220px;
  }
  .case-system-palette > span {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 1rem;
  }
  .case-system-palette b,
  .case-system-palette small { font-size: 10px; line-height: 1.3; }
  .case-system-palette small { opacity: 0.68; }
  .case-system-palette .red { background: var(--case-red); color: white; }
  .case-system-palette .ink { background: #202020; color: white; }
  .case-system-palette .paper { background: #fff; color: #111; }
  .case-system-palette .gray { background: #747474; color: white; }
  .case-system-type {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: clamp(1.25rem, 3vw, 2rem);
    border-left: 1px solid var(--case-line);
  }
  .case-system-type span,
  .case-system-type em { color: var(--case-gray); font-size: 10px; }
  .case-system-type strong,
  .case-system-type p { font-family: var(--campaign); }
  .case-system-type strong {
    margin-top: auto;
    font-size: clamp(1.4rem, 3vw, 2.6rem);
    line-height: 1;
  }
  .case-system-type p {
    margin: 1rem 0 0.25rem;
    color: var(--case-red);
    font-size: clamp(1.2rem, 2.4vw, 2rem);
    font-weight: 700;
    line-height: 1;
  }
  @media (max-width: 780px) {
    .case-system-sequence { grid-template-columns: repeat(2, minmax(0, 1fr)); }
    article:nth-child(3) { border-top: 1px solid var(--case-line); border-left: 0; }
    article:nth-child(4) { border-top: 1px solid var(--case-line); }
    .case-system-foundations { grid-template-columns: 1fr; }
    .case-system-type { min-height: 240px; border-top: 1px solid var(--case-line); border-left: 0; }
  }
  @media (max-width: 500px) {
    .case-system-sequence { grid-template-columns: 1fr; }
    article + article { border-top: 1px solid var(--case-line); border-left: 0; }
    .case-system-stage { aspect-ratio: 16 / 10; }
    .case-system-palette { grid-template-columns: repeat(2, 1fr); min-height: 300px; }
  }
</style>
